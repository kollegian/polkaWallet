This is an example repo to connect polkadot js wallet to cypress. 

You can use with 
```
npm install 

cypress run --spec cypress/e2e/3-\ polkaWalletExamples/polkaWallet.cy.js --browser chrome --headed
```
